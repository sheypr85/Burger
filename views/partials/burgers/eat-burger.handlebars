
<div class="row">
	<div class="col md 4">
		{{id}}
		{{burger_name}}
	</div>

	<div class="col">
		<button class="btn btn-light" data-id="{{id}}" data-devournew="{{devour}}">
			EAT THE BURGER!
		</button>
	</div>
</div>